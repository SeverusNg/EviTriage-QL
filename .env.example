# Gate A requires no environment variables, API keys, or network services.
#
# `evitriage doctor --json` discovers Python, Java, and CodeQL from the process
# environment and PATH. Install optional tools outside this repository.
#
# Future LLM credentials must be read from environment variables or a secret
# manager and must never be committed. No LLM provider is implemented in Gate A.
