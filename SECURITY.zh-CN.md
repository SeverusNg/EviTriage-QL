# 安全策略

[English](SECURITY.md) | [简体中文](SECURITY.zh-CN.md)

## 支持的版本

EviTriage-QL 是研究软件。我们会尽力为最新的 `0.1.x` 版本和主开发分支
提供安全修复。

| 版本 | 是否支持 |
| --- | --- |
| `0.1.x` | 是 |
| 更早版本 | 否 |

经过 Gate F 加固的离线纵向版本不得被视为生产级漏洞分类服务。

## 报告漏洞

不要在公开 issue 中包含利用细节、秘密、私有源码或尚未披露的第三方漏洞
信息。请使用仓库托管平台的私有安全公告通道，或维护者明确公布的其他私有
通道。如果没有可用私有通道，请仅在公开渠道询问维护者的私密联系方式，
不要披露技术细节。

在安全可行时，请提供：

- 受影响的 commit 或 release，以及操作系统；
- 使用合成数据的最小复现；
- 预期和实际观察到的安全边界；
- 影响及已知前置条件；
- 可用的缓解建议。

维护者会根据实际可用性确认并协调处理；本研究项目不承诺固定响应 SLA。
请在公开细节前留出修复和协调披露时间。

## 相关信任边界

操作者选择 ProjectSpec，但其中的内容、目标源码树、注释、构建文件、SARIF
文档及未来模型输出都会跨越不可信边界。值得报告的安全问题包括但不限于：

- 从受管根目录发生路径遍历或符号链接逃逸；
- 修改原始目标源码树；
- 通过配置或仓库文本执行 shell 或注入命令；
- 在日志、诊断、manifest、数据库或报告中泄露秘密；
- 接受未知字段或改变权限的 ProjectSpec 字段；
- 不安全的 SQLite/数据库 URL 处理；
- 虚假报告 CodeQL 扫描或有证据支持的裁决；
- 接受属于另一项目、源码快照、SARIF 产物或告警出现位置的补充证据；
- 未来的模型或仓库能够改变工具权限或工作流目标。

`ingest-sarif` 永不执行目标代码。`scan` 只能通过受约束的 CodeQL 运行器
执行显式配置且已检入的 Maven Wrapper，但 Gate B 尚未提供完整的操作系统
文件系统/网络/资源沙箱。真实扫描只能用于可信夹具，或运行在不含有价值
凭据的一次性外部沙箱中。

`make security-test` 会运行针对冻结 Gate F 攻击类别的离线回归。模型
payload 在工作流和远程提供方边界都会接受确定性的凭据模式脱敏，但原始
本地审计产物会有意精确保留证据。模式匹配可能漏掉新型或未标记的秘密；
应把工作区和报告视为含源码的机密数据。

## 负责任使用

只能将本软件用于已获得授权的系统和代码。项目不会自动关闭告警、生成
exploit 或授权攻击性测试。请遵守适用法律、目标项目披露政策、数据集
许可证和 CodeQL 自身许可条款。
